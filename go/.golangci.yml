# TRIAL — candidates on, to be narrowed after triage.
version: "2"

linters:
  default: standard
  enable:
    - unconvert
    - unparam
  settings:
    govet:
      enable:
        - nilness
        - shadow

formatters:
  enable:
    - gofmt
